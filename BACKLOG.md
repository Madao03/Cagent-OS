---
name: cagentos-backlog
purpose: 集中记录已识别但明确推迟的项 —— 防止遗忘，也防止上线前漂移
updated: 2026-07-27
status: 上线前只做「🔴 上线阻塞」区；其余全部推迟
---

# CagentOS Backlog

> **使用原则**：上线前只碰「上线阻塞」区。以下所有项都已评估为「不影响朋友能用 + 我能看到他问什么」这个完成定义。
> 每项都标了触发时机，不要因为"顺手"提前做。

---

## 🔴 上线阻塞（不在 backlog，是待办 —— 列在此处仅供对照）

```
① 成本熔断 + 限流       半天  ← 唯一会真金白银出血的
② 埋点：原始 query 落库  半天  ← MVP 目的就是学习，不落库=白上
③ 免责声明（UI + prompt） 1小时 ← 保护自己，朋友圈+投资+亏钱=关系风险
④ 密码哈希确认 bcrypt/argon2 + git 历史无 secret  半小时
⑤ Golden Cases ×13 跑一次存基线  1小时
⑥ 部署：Caddy + systemd + SQLite备份 + 告警  1天
```

---

# A. 溯源系统（P4–P6 + 新增）

## A1. dump 脚本按数组下标猜期间 🟢
**现状**：诊断脚本把 registry 里的 fact 按数组顺序分组标 Q1/Q2/Q3，导致标签错位
（XPEV dump 曾把 Q4'24 标成 Q1 FY2025，把 FY2024/FY2025 标反）。
**修法**：按 `period_start` / `period_end` 分组，不按下标。
**触发**：下次需要 dump 分析时。
**注意**：registry 数据本身是对的（卡片显示正确），只是 dump 展示错。

## A2. 派生 fact 成对出现 + 丢失 period_type 🟢
**现状**：percentage bridge 使每个比率注册两条（`0.3818` 和 `38.18`），
且派生 fact 无 `period_type`，卡片展开时无法显示"这个同比是哪两期之间的"。
**修法**：① 标注哪条是主值；② 派生 fact 继承 parents 的 period 信息。
**触发**：做派生卡片交互展开（②）时。

## A3. 茅台全量 10,204 facts 注册 🟢
**现状**：`ashare.report` 注册 1998–2026 全历史（28 财年 + 25 Q1 + 24 累计），
用户只问"最新净利润"。
**风险**：registry 越大，post-hoc derived 反推凑中无意义组合的概率越高（假 derived）。
**修法**：按需截取（如最近 N 期），或按 query 涉及的期间过滤。
**触发**：出现假 derived 误判，或性能问题时。

## A4. post-hoc derived 判定仍偏宽 🟢
**现状**：已排除绝对金额与 |value|<100 裸整数，但仍是"能否由 registry 任意两数算出"的事后反推。
registry 有几十条 fact 时，两两组合数百种。
**根治**：P1 派生链已实现 agent 显式声明 `[derivations]`，
待覆盖率提升后，post-hoc 反推可完全下线，改为「未声明即 untraced」。
**触发**：`derived_traced` 稳定覆盖大部分比率之后。

## A5. text_citation 符号解析不一致 🟢
**现状**：同一数字"同比下降 17.6%"在不同句式下解析出 `+17.6` 和 `-17.6` 两条 fact，
导致 `sign_conflict` 误报。
**修法**：注册层按 `(source, 原句, 绝对值)` 去重；符号矛盾在**注册时**标记，不留到匹配时。
**触发**：sign_conflict 误报影响判读时。

## A6. Fact 独立索引表 🟢
**现状**：facts 随 `run.provenance` 事件落进 `events` 表（功能上不丢数据），
但无独立索引，无法按 `(ticker, period, caliber)` 直接查。
**修法**：建独立 fact 表 + 索引。
**触发**：复盘功能正式开工时。
**⚠️ 届时需要一次性从 events 反解析回填历史 facts。**

## A7. RAG / docs.read 的 text_citation 注册 🟢
**现状**：已修（2026-07-28）。修复前 RAG 命中但 traced_text=0，
知识库内容与模型编造的数字在输出中视觉相同。
**修法**：`docs.read` 加入 `_TEXT_CAPABILITIES`，`_extract_from_text` 和
`_extract_from_dict` 均处理知识库来源的字符串内容为 `verified_citation` fact。
卡片标记 `source="knowledge_base"` + `source_tier="curated"`。
**待观察**：`curated` tier 的卡片是否与 EDGAR/websearch 卡片视觉可区分。
**触发**：出现知识库数字仍被标 untraced 时。

---

# B. EDGAR / LANE 2

## B1. iXBRL / ex992 格式无法提取 🟠
**现状**：XPEV Q1 2025 / Q2 2025 / Q1 2026 的业绩稿是 iXBRL 内嵌格式（226 个 table），
HTML 表解析器返回 0 records。ex991 正常，ex992 全部失败。
**下游代价**：每个取不到的季度都会把 agent 推向不可验证的文本路径，制造大批 untraced 数字
（XPEV Q1 2026 那次 21 个 untraced 全是这个原因）。
**★未做的 5 秒诊断**：
```bash
grep -c "ix:nonFraction" d920964dex992.htm
```
- **有命中** → iXBRL 反而**更容易解析**：期间/单位/符号全是机器标注的，
  不需要表头猜期间、不需要括号负数处理、不需要行归一化 → 可能是「少写一半代码」
- **零命中** → 226 个 table 是港交所式深层嵌套排版，才是真难
**触发**：随时（诊断成本 5 秒）。SEC 整体在推 inline XBRL，这类文档只会越来越多。

## B2. 非 12 月财年的季度窗口对齐 🟠
**现状**：AAPL（财年结 9 月）、NVDA、BABA（3 月）的财年季 vs 日历季存在标签错位。
覆盖率统计中 AAPL Q4 2025 曾 NOT FOUND。
**修法**：季度窗口按公司实际财年结日推算，不按日历季末。
**触发**：需要覆盖非 12 月财年公司的季度数据时。

## B3. BABA / 长期上市 FPI 的 submissions 分页 🟠
**现状**：`submissions.recent` 上限约 1000 条。阿里 2014 年上市 + 港股双重上市，
6-K 数量极大，可能溢出 → **拿不到完整历史（数据不全，不只是慢）**。
**修法**：用 `filings.files[]` 分页文件取历史。
**触发**：需要长期上市 FPI 的历史季度数据时。

## B4. S3 Phase 2 LLM 兜底分类器 🟢
**现状**：零成本正则信号已覆盖近期季度（XPEV 22 季 sweep 精确率高）。
LLM 兜底用于标题措辞古怪的边缘情况。
**难负样本已备**：`xpev_6k_q32022_hard_negative.html`（港交所中期报告，有完整财务表格）
——这是唯一真实撞出来的难负样本，比人造的有代表性。
**触发**：出现分类器漏报/误报影响覆盖率时。

## B5. LANE 3（10-K/20-F 正文定性）⚪
MD&A / 风险因素 / 附注。RAG 已能吃文档，MVP 不需要。
**触发**：有明确的定性深度需求时。

---

# C. A股 Adapter

## C1. 勾稽⑤ 未纳入分红项 🟠 ★重要
**现状**：`ΔRE ≈ 归母净利润` 在茅台 2026Q1 精确成立，
**但那是因为该区间（12/31→3/31）恰好没有分红入账。**
完整等式应是 `ΔRE ≈ 归母净利润 − 分红 − 其他权益调整`。
**风险**：跨越分红除息日的区间（如比到 6/30 或 9/30，茅台分红通常在 6-7 月）
会差出一个分红量级 → **判 FAILED，把一份好数据误杀。**
**修法**：
```
① 纳入分红项（利润分配表 / 现金流量表的「分配股利、利润」）
② 若 Sina 源拿不到分红明细 → 降级为
   「ΔRE 与归母净利差异 ≤ 一个分红量级则通过」，且 flag 而非 fail
```
> 宁可宽，不可误杀 —— 同「真实世界的合法断层要 flag 不要 reject」纪律。
**触发**：⚠️ **上线后第一次遇到跨分红期的 A股 查询就会触发**，优先级高于本区其他项。

## C2. 单位归一化路径未被执行 🟢
**现状**：Sina 源直接返回「元」，万元 ×1e4 / 亿元 ×1e8 的换算代码路径从未真实跑过
（已有 16 条单测覆盖，但无真实数据经过）。
**风险**：Sina 改格式或换接口时，这是未经真实验证的代码。
**修法**：fixture 里放一份万元/亿元样本走真实路径。

## C3. 业绩预告 / 业绩快报（鲜度层）🟠
**现状**：未实现。
**价值**：预告比年报早约 3 个月；且**自带「实际 vs 预期」能力**
（预告给区间 → 年报给实际 → 后验比对），与 EDGAR guidance 后验同构，且免费。
**触发**：朋友开始问"业绩预告怎么看"，或需要 A股 鲜度层时。

## C4. 追溯重述检测 + 送转股复权 🟠
**追溯重述**：会计政策变更/差错更正导致上年同期数被重述 → 同期间不同报告数字不同。
修法同 LANE 2：`(代码, 期间, 科目)` 去重，值不一致标 `restated=true` 保留双值。
**送转股**：改变股本 → EPS / 每股净资产在时间序列上不可比，跨除权日同比需复权。
**触发**：出现历史数据前后不一致，或做 A股 EPS 时间序列时。

## C5. 巨潮 PDF 解析（升级为一手源）⚪
**现状**：只给公告索引 URL（可追溯），不解析 PDF。akshare 标 `tier=secondary`。
**触发**：需要一手权威 A股 财报数字时（≈重做一遍 LANE 2 的工作量）。

## C6. BS 列名扩展 🟢
**现状**：已改为**来源表驱动**（fail-closed），列名变化不再影响差分正确性。
仅少数科目在展示层可能缺名。
**触发**：发现具体缺失科目影响输出时。

## C7. 新上市标的 P/B 分母 🟢
**现状**：已在 skill 中要求区分募资前/募资后净资产。
**工程侧可选增强**：`ashare.report` 若能取到募资额与最新净资产，
可自动产出募资后 P/B，避免依赖 LLM 判断。
**触发**：频繁分析新上市标的时。

---

# D. 港股

## D1. 纯港股财报（HKEXnews）⚪
**现状**：路由层已完成（`has_sec_cik()` + `not_sec_registered` + 制度性说明），
**中概股走 EDGAR（季度）、A+H 走巨潮（季度），二者数据质量优于 HKEX 通道。**
未覆盖的仅剩**纯港股**：腾讯 00700 / 汇丰 / 友邦 / 长和 / 港交所 00388。
**成本高回报低**：HKEXnews 以 PDF 为主，且港股主板**仅强制半年披露**。
**修法（若做）**：akshare 港股财报 + H2 差分（`H2 = 年报 − 中期`）+ 三表勾稽。
**注意**：财年结日高度多样（12/31、3/31、6/30、9/30 都常见），
**禁止默认 12/31**；A+H 存在 CAS/HKFRS 准则双轨，两套数字可合法不一致。
**触发**：朋友开始频繁问纯港股基本面。

## D2. 港股市值口径（W股 / H股 股本计算）🟠
**现状**：MiniMax vs 智谱 案例中两个市值均被低估。
MiniMax 分析 640–680 亿 ｜ 实际 776.7 亿（7/21 收盘 222.4 港元）
智谱 分析 5,200–5,960 亿 ｜ 实际 ~9,060 亿
市值比 分析 1:8~9 ｜ 实际 1:11.7
**价格是对的**（52 周区间 188–1,330、收盘 222.4 均正确），错的是股本口径。
**根因假设**：
MiniMax = W 股结构（A 类 / B 类普通股，同股不同权）
智谱 = H 股 + 未上市内资股/A 股（H 股仅为总股本一部分）
→ 用 H 股股本 × 股价 会系统性低估总市值
**修法**：市值优先取行情源直接返回的字段，不自行用「股价 × 某类股本」计算；
自算时必须注明所用股本口径（总股本 / H股 / 流通股）。
**⚠️ 影响面**：市值是 PE / PS / PB 全部倍数的分子，口径错会污染所有估值结论。
**触发**：分析港股/双重上市标的估值时。

---

# E. Crypto / 新闻层

## E1. RSS 新闻管线 🟠
**规格已备**：`CRYPTO_RSS_INGESTION.md`（完整）。
**杠杆最高的三个源是「模式」而非单个源**：
```
GitHub releases        任意仓库 /releases.atom
Discourse 治理论坛      任意论坛 /latest.rss（Aave/Uniswap/Compound 全是）
SEC EDGAR 申报          任意 CIK 的 Atom feed ← MSTR 8-K 直接喂 mNAV
```
**核心难点是跨源去重**（一条融资新闻同时出现在十家站；PANews 与 Odaily/BlockBeats
内容高度重叠，**必须同池去重**），否则会把回音室当多源确认。
**第 8 次同模式 bug 的预防位置**：`published_at` 缺失时**禁止用 `fetched_at` 兜底**
——会让所有历史文章显示为今天发布。
**触发**：需要新闻/叙事层，或做每日简报时。

## E2. mNAV（MSTR 等 crypto 敞口标的）🟢
**现状**：未实现，但**所有输入已具备**：
```
BTC 持仓量 ← 8-K 增持公告（LANE 2 已建）
股本/负债  ← 10-Q / 10-K（LANE 1 已建）
BTC 现价   ← Binance / 金十
mNAV = 市值 / (BTC持仓 × BTC价 − 净负债)
```
**不需要任何 crypto 数据供应商** —— 是 EDGAR 管线的白捡复用。
**触发**：朋友问 MSTR / 矿企。

## E3. Crypto P1/P2 源 🟢
```
P1  Bybit/OKX（资金费率跨所交叉验证，比方向不比数值）
    CoinGecko/CMC（市值互校）
    Deribit（期权 IV/DVOL/PCR —— 目前完全没有期权维度）
P2  Farside（ETF 流向备源，SoSoValue 免费期到期后的退路）
    L2Beat / Ultrasound.money / Hyperliquid / Mempool.space
```
**触发**：对应 skill 需求出现时。

## E4. MVRV-Z 分母定义与公开图表不一致 🟢
**现状**：字段已改名 `mvrv_z_variant`，元数据注明「分母为 `stdev(diff)` 而非
`stdev(MV)`，值偏高，不可与 Glassnode 等公开图表直接对照」。
skill 已改用 `mvrv_percentile`（尺度不变、跨周期可比）。
**常驻不变量测试已加**：全历史序列 `diff>0 → Z>0`（不依赖具体公式的数学必然性）。
**触发**：需要与公开图表对齐时才改公式。

---

# F. Skill 框架

## F1. 周期股三档 PE 未落地 🟠
**现状**：新版 skill 已要求「峰值/中周期/底部」三档并列，
但 CXMT 实测只给出两档（静态 308.9x / 动态 5.5x），缺中周期与底部。
**缺的是方法论口径，不是工程**：
```
✗ 正常化盈利怎么取？（一轮周期平均利润率 × 当前收入？同业 mid-cycle EPS？）
✗ 三档的具体口径与阈值
✗ 重资产周期股用 P/B 还是 EV/EBITDA 作主锚，参考区间多少
```
**动作**：把 CXMT 案例发给有经验的朋友，请其给方法论说明 → 翻译成 skill。
**触发**：朋友给出框架后。**这是投研判断，不该自己拍。**

## F2. 卖方锚未纳入（部分改善）🟢
**现状**：CXMT 实测**未用**野村目标价（116 元，基于 2028E EPS 5.8 元 × 20x），
但改用了**机构询价区间 7.26–65.19 元（350+ 家参与）**——这个锚实际更强
（350 家机构真实报价分布，一手信息，直接量化分歧）。
**待观察**：「搜到必须用」规则是否稳定生效。
**触发**：再次出现搜到关键锚却不用时。

## F3. skill 改名（`us-stock-analysis` → `equity-analysis`）🟢
**已完成的替代方案**：
```
✅ A. 放宽 prompt_compiler 匹配规则（个股/估值/财报，含美股/A股/港股）
✅ B. 新版 SKILL.md 的 description 已改为多市场
→ 改名的全部「实际收益」已拿到，剩下的是认知清晰
```
**若要改，必须一个原子 commit 全改**（半改比不改更糟）：
```
① 磁盘文件夹名 us-stock-analysis/          ← 易漏，第 10 处
② CLAUDE.md 技能表
③ prompt_compiler.py 匹配规则
④ permissions.py（dict key + skill_name 两处）
⑤ skill_io.py 类名 UsStockAnalysisInput/Output + 默认值
⑥ __init__.py import + __all__
⑦ ★向后兼容别名：接受 "us-stock-analysis" 并映射到新名
   → 历史数据可解析，漏掉的引用不静默失败
⑧ ★先 grep 全仓，含 Golden Cases 目录 / 知识库 md / prompt 模板
```
**⚠️ 不可逆成本**：`events` / `trace` / `memory` 已积累 `skill="us-stock-analysis"` 记录。
缓存可以 bump 版本重抽，**对话历史不能重跑** → 同一 skill 在数据里永久存在两个名字，
且复盘功能依赖 trace 回放。
**⚠️ 顺序**：必须「先改名 → 再冻 Golden 基线」。反了基线就断了。
**触发**：上线后，且确认 Golden Cases 无 skill 名引用。

## F4. Q1'26 净利数据待核 🟢
**现状**：同一题两轮给出 `247.6亿` 和 `330亿`，必有一错。
若 Q1=330亿，则 Q2 仅 170–240亿（H1 指引 500–570亿），
在 DRAM 涨价周期中环比下降不合理。
**核法**：查招股说明书 / 上市公告书（§2.1.2 定义的新上市标的唯一一手财务源）。
**触发**：随时，成本低。

## F5. 归母/少数股东口径混淆 🟢
**现状**：CXMT 案例中「25% 归少数股东」实际为 74%，方向说反。
已加 dq-guard 断言（比例之和为 1 + 文本一致性 + 过半时额外 flag）。
**待观察**：断言是否稳定触发。少数股东占比过半的标的（VIE 结构、
合并控制类、国资并表）都有此风险。
**触发**：再次出现口径方向错误时。

## F6. 同业对比 PE 基准逐列披露 🟢
**现状**：已在 skill §0.6 要求每列单独注明基准，输出模板「基准/期间」列改为必填。
**待观察**：LLM 是否稳定遵守（三列基准均需自行推断时容易省略）。
**触发**：再次出现只披露部分列基准时。

## F7. 「搜到必须用」第三次未生效 🟠
**现状**：MiniMax 案例中，搜索结果已包含以下关键信息，但输出未采用：
① MiniMax 7/10 已完成 160 亿港元融资（20+ 家长线/主权基金参与，
80% 用于 AI 基础设施与模型研发）；7/14 又配售 3560 万股
→ 而输出的空头论点之一是「再融资悬顶：至今没有大规模再融资消息」
→ ★该风险已落地，且落地方式良好 —— 空头论点被事实推翻
② 里昂重申智谱「跑赢大市」，目标价 2,061 港元（卖方锚，未纳入）
③ 同事件相反反应：智谱宣布 314 亿配售当天涨 11.34%；
MiniMax 次日宣布完成 160 亿融资当天跌 9.68%
→ ★这个对比比输出中列的五条原因都更有说服力
**累计三次**：CXMT 第一轮（野村目标价）、CXMT 第三轮（部分改善）、本次。
**推测根因**：搜索结果条数多时，agent 只用了前几条摘要，未读全量。
**待观察**：是否需要在 skill 中加「搜索结果中出现融资/目标价/一致预期/官方公告时，
必须逐条核对是否与已有论点冲突」。
**触发**：再次出现关键信息搜到不用时。

## F8. 同业倍数差不得降级为脚注 🟠
**现状**：MiniMax 案例中，用户问「为什么差这么多」，
最直接的答案在 `[derivations]` 块里且附「仅作对比参考」免责：
MiniMax PS ≈ 105x ｜ 智谱 PS ≈ 812x
两者营收规模相近（约 $79M vs ~$100M）
→ 8–11 倍市值差距几乎全部是估值倍数差，不是基本面差
而正文结论称「市值差距大部分是合理的」，
用 41% vs 25% 的毛利率差解释 8x 的 PS 差 —— **论证撑不住**。
且「两者发展阶段不同」不成立：营收规模相近即同一阶段。
**修法（skill）**：
当问题形式为「A 和 B 为什么差这么多 / 谁更贵」时：
① 同业倍数对比（PS/PE/PB）必须出现在正文，禁止仅置于 derivations
② 若倍数差远大于可解释的基本面差（如毛利率差），
必须显式指出「差距主要来自估值倍数，而非基本面」
③ 禁止用「发展阶段不同」笼统解释 —— 须给出可验证的阶段差异
（营收规模 / 客户结构 / 盈利阶段），营收规模相近时该理由不成立
**触发**：F1（周期股三档 PE）一并处理时。

---

# G. 产品功能

## G1. Watchlist 🟠
**推迟理由**：还不知道朋友会盯什么。
**关键价值**：接上 CronAgent 日报 → 从「我想起来才用」变成「它每天来找我」，
这是粘性的来源。
**依赖**：需要真实使用数据先告诉你他们关注什么。
**触发**：上线观察两周后。

## G2. 自动简报 🟠
**先做人工版（零代码）**：每周你自己跑一次系统 → 挑值得看的 → 群里发一条。
**测什么**：有没有人回复？有没有人问"能不能天天发"？有没有人说"我更想看 XX"？
**理由**：pull 产品（问答）在没有习惯时会静默死掉；push 产品建习惯。
知道要什么内容再建管线，比先建管线再猜内容效率高一个量级。
**触发**：人工版有正反馈后。

## G3. 复盘功能 🟠
**已建约 80%**：`memory.save_thesis` / `query_theses` / `check_contradictions`（LLM 语义比较）
+ Fact registry 已落 events 表。
**完整形态**：三个月前的论点 → 当时看到的数据 → 今天重新取同期数据 →
哪个假设被打破了。
**这是通用 LLM 做不到的事** —— 需要持久化结构性记忆 + 可回放数据溯源。
**已有先例验证**：XPEV guidance 后验（Q4'25 抽出的指引区间，两个月后 Q1'26 实际
四项全中）——那就是一次机器执行的复盘。
**依赖**：A6（Fact 索引表）
**触发**：上线后第一个迭代。

## G4. Portfolio 持仓/收益率 ⚪ 明确不做
```
投顾红线：会自然把对话推向「我该不该卖」
计算风险：收益率算错直接摧毁前面所有数据可信度
```
**不设触发条件。** 若将来做，需先解决合规定位。

## G5. 反馈捕获 👍/👎 🟠
**不阻塞上线**，但 N=10 时需要知道哪个回答有用。
**最低形态**：每个回答带 👍/👎 + 一句话输入框 → 写进 trace。
**价值**：Golden Cases 才能用**真实失败案例**持续扩充，而不是自己想出来的。
**触发**：上线后第一周。

## G6. 溯源前端 ②④ 🟢
```
② 派生展开    [derivations] 块已以文本形式显示（能回答"38.2% 怎么算的"），
              做成可点开卡片是体验优化，不是能力补齐
④ 来源角标    新闻层（RSS）未接，text_citation 量小；
              腾讯那题已用 ⚠️ + "不做推算" 覆盖最坏情况
```
**触发**：④ 等 RSS 新闻层接入后重评。

---

# H. 工程 / 运维

## H1. 意图分类器 🟢
**设计已讨论**：零成本信号先行 → LLM 兜底 → 失败退化。
**当前替代**：prompt 里的路由规则（建议性）+ 代码层硬路由（`has_sec_cik` 等）。
**已验证的教训**：**规则写在 prompt 里是建议性的，写在代码里才是结构性的**
（FRED 那次：21 个系列在系统里，agent 从记忆答利率）。
**触发**：路由错误频繁到影响体验时。

## H2. 测试分层维护 🟢
```
Tier 1  fixture-only，-m "not sec"，39 条 / 2.9s，每次提交
Tier 2  选择层（26 条 accession 钉死）+ SEC 回归（13 条），定时跑
```
**关键**：Tier 1 必须保持秒级 —— 慢测试的结局都是没人跑，
那几条反例基线的价值就废了。
**注意**：正向断言会随财报更新失效（如"XPEV 最新年度 = FY2025"），
**反例基线（不该出现的错误值）永久有效**，优先维护后者。

## H3. `tier` 字段三义 🟢
```
Fact.media_tier          信源可信度（0_primary/1_media_pro/3_aggregator）
Fact.source_tier         数据源层级（primary/secondary）
DataSourceAdapter.tier   适配器降级优先级（int）
```
三者在不同类上，冲突风险低，但**三个月后你会对着 `tier` 想半天是哪个**。
**动作**：已记入 CLAUDE.md 即可，无需改名。

## H4. akshare / 巨潮 接口漂移监控 🟠
**风险**：`stock_financial_report_sina` 与巨潮网页接口**均非正式 API**，随时可变。
**修法**：Tier 2 加 schema 漂移检测（字段名/结构变化告警）。
**触发**：上线后。第三方改版会静默改字段，比报错更难发现。

---

# J. 知识库 / RAG

## J1. 持续注入缺失（定位问题的根因）🟠
**现状**：29 篇人工分诊内容，无自动注入回路，依赖手动 read-later + content-triage。
**症状**：查询命中率低，纯数字类问题返回噪音 → 本轮已通过条件路由缓解。
**解药**：E1（RSS 管线）已有完整规格，其两级漏斗正好是「自动发现 + 人工标准」：

RSS 拉清单（便宜） → content-triage 五维评分 → 仅 A/B 类 fetch 全文 → 入库

**★定位说明（建议写进 CLAUDE.md）**：

本知识库是「精选深度层」，不是全量企业知识库。
价值 = 别人搜不到的深度内容（付费研究 / 微信公众号 / 分诊后的研报），
而非覆盖率。29 篇精选 > 1000 篇抓取（对个人投研工具而言）。
因此不应与企业 RAG（靠量 + 爬虫）对标。

**触发**：E1 开工时。

## J2. RAG 条件路由（本轮已改）🟢
**已做**：会话级 status 缓存 · 纯数字问题跳过 · 相似度地板 0.5 代码层硬过滤 ·
定性问题保持高优先级。
**待观察**：0.5 地板是否过严（是否出现应命中却被丢弃的情况）；
条件路由的判据是否会误判混合型问题。
**触发**：出现该命中未命中，或数字问题仍被 RAG 污染时。

## J3. 知识库内容的时效衰减 🟢
**现状**：RAG 结果含 date 字段，skill 已要求「≥7 天标⚠️可能过时」，
但未强制。深度研究类内容（L3）的合理时效是月级，与新闻不同。
**修法**：按 chunk_type 分级时效

research / 深度研报 → 6 个月内有效
news / 快讯 → 7 天
ledger / 台账 → 无时效

**触发**：出现引用过时研报做当期判断时。

---

# I. 明确不做（写下来防漂移）

```
✗ 继续压低幻觉率            0% 已到顶（n=3 可复现），再往下只能靠「少说话」换
                            —— 数据已证明：收紧会让 data_numbers 从 18 掉到 5
✗ 继续优化派生链            端到端已通，精度继承闭环，够用
✗ Portfolio 持仓/收益率      投顾红线 + 计算风险
✗ X/Twitter 接入            无免费 API / 无稳定 RSS；
                            不用不可靠的第三方镜像凑数，明确列为不覆盖
✗ Telegram / Discord        项目公告常首发于此，但 RSS 不可得
✗ 自建 GPU 跑 embedding      MVP 走 API 更便宜（已是 API）
✗ 仪表盘 / 漏斗分析          N=10 时任何看起来像趋势的东西都是噪音；
                            埋点只保证「数据不丢」，
                            上线一周后找 2-3 人打 15 分钟电话，
                            或看着某个人用一次 —— 信息量抵得过一个月仪表盘
```

---

# 附：可复用的工程纪律（不是待办，是已固化的判断）

```
① 信息缺失或歧义 → 返回 None / unknown / 抛错，禁止填默认值
   默认值只允许在消费层，且必须显式标注
   —— 已发生 8 次同模式 bug（fiscal_year硬编码 / fy误用 / 别名断档 /
      币种默认USD / bare year→Dec31 / 币种unknown / find()无下限 / 假精度）

② 三态语义：null（不适用）≠ UNKNOWN（应有但未知）≠ 具体值
   压成两态会静默通过本该拦截的冲突

③ 先对口径，一致才谈方差
   口径不同 = 两个不同的指标，分别落库，不进同一去重组，不报警

④ flag vs reject
   flag   → 可能异常但可能合法（政策断层/并购/准则变更/审计调整）
   reject → 逻辑上不可能（符号矛盾/filed_date早于期末/跨币种聚合/跨准则运算）
   真实世界的合法断层要 flag，自动丢弃会在最有信息量时扔掉数据

⑤ 用结构信号，不用数值启发式
   从列头拿期间 > 从正文猜；从来源表判定流量/时点 > 从科目名匹配
   fail-closed 而非 fail-open

⑥ 组件能跑 ≠ 端到端有效
   已发生 6+ 次「指标名与实际测量对象错位」：
   "100%精确率"测的是「返回了东西」；39条绿灯测不到 find()；
   "幻觉率0%"测的是校验器；V1/V2 对比换了尺子；
   "19个幻觉"测的是注册完整性；"派生链通了"时 derived_traced=0
   → 每次报指标前问一句：它测的真是它声称的东西吗？

⑦ 测试的两层
   反例基线（盯不该发生的事）永久有效 > 正向断言（会随数据更新失效）
   fixture 离线秒级（每次提交）+ 少量实时 canary（定时）

⑧ 别用「已知限制」「随机性」关闭调查
   已两次证明是别的原因（iXBRL 边界 / accession 传参丢失）

⑨ 规则写在 prompt 里是建议性的，写在代码里才是结构性的

⑩ 「我没有这个数据」是功能不是失败，必须显式呈现
```
